<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--Mybatis에게 Mapper 경로를 적어야 한다. -->
<mapper namespace="com.Increase.emotionDiary.mapper.CalendarMapper">
	
	<select id="selectList" resultType="List">
	SELECT
		calendar_id AS calendarId,
		member_id AS memberId,
		calendar_emotion AS calendarEmotion,
		create_at AS createAt
	FROM calendar
	
	</select>

	<select id="selectOne" resultType="CalendarVO">
	SELECT
		calendar_id AS calendarId,
		member_id AS memberId,
		calendar_emotion AS calendarEmotion,
		create_at AS createAt
	FROM calendar
	WHERE calendar_id = #{calendarId}
	</select>
	
	<insert id="insertEmotion">
	INSERT INTO calendar
	(
		member_id AS memberId,
		calendar_emotion AS calendarEmotion
	)
	VALUES
	(
		#{memberId},
		#{calendarEmotion}
	)
	</insert>
	<update id="updateCalendar">
		UPDATE calendar
			SET  calendar_emotion= #{title}
		WHERE calendar_id = #{calendarId}
	</update>
	
</mapper>